---
on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main
permissions: write-all
jobs:
  release:
    name: release
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3
        